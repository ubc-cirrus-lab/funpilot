apiVersion: extensions.istio.io/v1alpha1
kind: WasmPlugin
metadata:
  name: rq-metrics
  namespace: istio-system
spec:
  selector:
    matchLabels:
      istio: ingressgateway
  url: oci://docker.io/changyuan/funpilot-wasm-plugin:latest
  imagePullPolicy: Always
