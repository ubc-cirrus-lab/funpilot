apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: app-1
spec:
  template:
    spec:
      containers:
      - image: docker.io/changyuan/funpilot-example-workload:latest
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: 500m
            memory: 250M
